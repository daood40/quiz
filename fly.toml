# Fly.io: `fly launch --no-deploy` then `fly postgres create` + `fly postgres attach`,
# `fly secrets set JWT_SECRET=$(openssl rand -hex 64) CORS_ORIGIN=https://<app>.fly.dev`, `fly deploy`.
app = "quiz-platform"
primary_region = "fra"

[build]

[env]
  NODE_ENV = "production"
  PORT = "3001"
  JOBS_ENABLED = "true"

[http_service]
  internal_port = 3001
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 1

[[http_service.checks]]
  interval = "30s"
  timeout = "5s"
  grace_period = "20s"
  method = "GET"
  path = "/health"
