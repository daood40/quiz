# Quiz Platform
منصة أسئلة ومسابقات ثنائية اللغة (عربي/إنجليزي): موقع + PWA + تطبيق Android/iOS بنفس الكود.

## المكدّس
React 18 + Vite (web/) · Fastify 5 + TypeScript + PostgreSQL 16 (server/) · Capacitor 7 (web/android، web/ios). CSS خام برموز في أعلى `web/src/styles.css`. كل النصوص من `web/src/i18n.tsx`.
هذا المشروع **ليس Flutter**: مهارات `flutter-*` مرجع فقط (memory/DECISIONS.md ADR-005)؛ للويب افتح `web-*`، وللواجهة `design-*`.

## قواعد لا تُخالَف
- لا مفاتيح سرّية في web/ ولا في سير العمل؛ الأسرار عبر البيئة فقط.
- الصلاحيات والتصحيح والنقاط على الخادم؛ الواجهة تعرض.
- كل شاشة لها أربع حالات: تحميل (skeleton)، فراغ، خطأ مع إعادة محاولة، بيانات.
- خصائص CSS منطقية فقط (inset-inline، margin-inline…) لا left/right.
- localStorage عبر `storageGet/storageSet` من `web/src/api.ts` فقط.

## قبل أي push
`npm run lint` ثم `npm test` (يحتاج PostgreSQL) ثم `npm run build` ثم دخان `npm run e2e --workspace=web`. لا تدفع أحمر. ما لا يُشغَّل هنا (gradle/xcodebuild) يُشغَّل في GitHub Actions.

## مراجع
الحالة: `memory/PROJECT_MEMORY.md` · المتبقي: `memory/TODO.md` · الإطلاق: `docs/LAUNCH.md` · المتاجر: `docs/STORE_SUBMISSION.md` · الأدلة: `docs/GATES.md`.

## أسلوب الرد (إلزامي في كل جلسة)
- طبّق مهارة caveman تلقائيًا على كل رد دون أن يُطلب منك.
- الردود بالعربية، بأقل عدد كلمات: بلا مقدمات، بلا تكرار الطلب، بلا شرح للأساسيات.
- لا تعرض الكود الذي كتبته في الرد؛ اذكر اسم الملف فقط.
- رسائل commit بأسلوب caveman-commit.
- الاستثناء الوحيد: إن طلبتُ "اشرح بالتفصيل" فأجب بالتفصيل في ذلك الرد فقط.

## مهارات Flutter (‎.claude/skills/flutter-*‎)

- المستودع فيه 34 مهارة Flutter مثبّتة. افتح المهارة المناسبة **قبل** كتابة الجزء
  الذي تخصّه، لا بعده. الفهرس والترتيب في `flutter-app-blueprint`.
- عند طلب ميزة كاملة أو تطبيق كامل: اتبع `flutter-autobuild` — خطة في
  `docs/SPEC.md` ثم تنفيذ المراحل بالترتيب، بلا توقف للسؤال بين المراحل،
  وبلا `TODO` ولا شاشة فارغة ولا زر لا يعمل.
- عند التعارض: توجيهات هذا المستودع أعلاه ومستندات `docs/` **تسبق** أي افتراض
  في المهارات (المكدّس التقني، الألوان، التسمية، سير العمل) — المهارات مرجع
  تقني لا مصدر قرار.
- ثوابت لا تُخالَف: كل نص ظاهر من ملف الترجمة، كل شاشة لها الحالات الأربع
  (تحميل/فراغ/خطأ/بيانات)، `EdgeInsetsDirectional` لا `left/right`،
  لا مفاتيح سرية في التطبيق، الصلاحيات تُفرض على السيرفر لا في الواجهة.

## طريقة العمل والمهارات

- ابدأ كل جلسة بقراءة `skills-index` ثم `memory/PROJECT_MEMORY.md` و`memory/TODO.md` و`memory/BUGS.md`.
- افتح المهارة **قبل** كتابة الجزء الذي تخصّه. عند الشك افتح الأضيق نطاقاً لا الأوسع.
- طبّق `simplicity-first` على كل قرار بنيوي: أبسط حل يعمل أولاً، لا بناء للمستقبل المتخيَّل.
- عند طلب تطبيق أو ميزة كاملة: اتبع `flutter-autobuild` بلا توقف للسؤال بين المراحل، وبلا TODO ولا شاشة فارغة ولا زر لا يعمل.
- قبل قول "تم": بدّل الدور باتباع `qa-engineer-mode` وحاول كسر ما بنيته، ثم أعطِ تقرير PASS/FAIL.
- ممنوع قول "جاهز للإنتاج" قبل اجتياز بوابات `qa-production-readiness` بتقرير حقيقي.
- لا ادعاء نجاح بناء أو اختبار لم يُشغَّل. ما لا يُشغَّل هنا يُنقل إلى GitHub Actions.
- حدّث ملفات `memory/` في نفس الـcommit الذي أحدث التغيير.
- العمل على فرع دائماً، وcommit صغير برسالة تشرح السبب.
- ثوابت: لا مفاتيح سرّية في التطبيق، الصلاحيات تُفرض على الخادم، كل نص ظاهر من ملف الترجمة، أربع حالات لكل شاشة.
- المستخدم يعمل من الجوال فقط: لا أوامر طرفية محلية.
- عند التعارض: هذا الملف و`docs/` و`memory/` تسبق أي افتراض في المهارات.
