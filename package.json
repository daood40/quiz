{
  "name": "quiz-platform",
  "private": true,
  "version": "1.1.0",
  "description": "QUIZ PLATFORM — production-grade quiz & competition platform",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=server & npm run dev --workspace=web",
    "build": "npm run build --workspace=server && npm run build --workspace=web",
    "test": "npm test --workspace=server",
    "migrate": "npm run migrate --workspace=server",
    "seed": "npm run seed --workspace=server",
    "lint": "npm run lint --workspace=server && npm run lint --workspace=web && npm run lint:eslint",
    "lint:eslint": "eslint server/src web/src server/test"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.10.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "typescript-eslint": "^8.69.0"
  }
}
